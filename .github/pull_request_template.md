## Summary

- 

## Verification

- [ ] `npm run verify`

If any part of `npm run verify` was not run, say which command was skipped and why.

## Tester impact

- [ ] README/GUIDE updated when user-facing commands, launch paths, lifecycle semantics, or safety behavior changed.
- [ ] No generated or runtime files committed (`dist/`, `node_modules/`, `.env*`, `*.sqlite*`, logs, worktrees).
